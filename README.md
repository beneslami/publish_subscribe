## Publish-Subscribe Design Pattern

This repo is the implementation of the pub-sub design pattern in C/C++. The way the library is used can be found in example directory. All the relative
implementations are in respective directories. 